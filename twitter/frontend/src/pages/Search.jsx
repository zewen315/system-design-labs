export default function Search() {
  return (
    <div className="page">
      <h2>Search</h2>
      <p className="placeholder-note">
        Not built yet — there's no search endpoint on either service. Coming as its own
        focused piece of work.
      </p>
    </div>
  );
}
